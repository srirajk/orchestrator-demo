{
  "domain_id": "insurance",
  "display_name": "Insurance",
  "coverage": {
    "discover_url": "${INSURANCE_COVERAGE_URL}/coverage/{principal_id}",
    "check_url": "${INSURANCE_COVERAGE_URL}/coverage/{principal_id}/resources/{id}",
    "resolve_url": "${INSURANCE_COVERAGE_URL}/entities/resolve",
    "cache_ttl_seconds": 30
  },
  "memory_compaction": {
    "must_preserve": ["policy_id", "claim_id", "domain"],
    "can_drop": ["raw_agent_outputs", "routing_decisions"]
  }
}
