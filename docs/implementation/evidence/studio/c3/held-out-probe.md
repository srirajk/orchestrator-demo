# C3.2 — HELD-OUT unseen-policy probe (labeled: NOT part of the 31-item corpus)

> Auto-generated by `KnownBadCorpusCatchRateTest.heldOutUnseenPolicyProbe`. These are bad
> POLICIES the corpus never contained, paired with EXISTING intents, run through the SAME
> trained gate (seeded oracle + probes + validator + real pinned-Cerbos `0.53.0` compile).
> Only the candidate YAML is unseen — the gate is exactly the one measured over the 31.
> This is a SLIVER of unseen-policy signal, not a completeness claim.

## Headline

- **Held-out catch-rate: 100.0% (5/5)**
- **Held-out moat-critical (oracle generalized to unseen policy): 1**

## Held-out items

| ID | Class | Caught by | Caught | Moat-critical | Compile ran | Note |
|---|---|---|---|---|---|---|
| HO-01 | wildcard | VALIDATOR | yes |  | yes | Unseen: chat_user gets actions:['invoke','invoke_membership','*'] — a wildcard hidden in a finite rule. |
| HO-02 | cross_tenant | VALIDATOR | yes |  | yes | Unseen: targets sibling tenant 'delta' (corpus used 'beta') — out-of-subtree escape. |
| HO-03 | incomplete_fallthrough | VALIDATOR | yes |  | yes | Unseen: drops (register, platform_admin) — that base tuple falls through to the parent ALLOW. |
| HO-04 | wrong_role_segment | ORACLE | yes | yes | yes | Unseen moat: validator-clean, but leaves chat_user invoke_membership ALLOW — only the intent-built oracle denies it. |
| HO-05 | wildcard | VALIDATOR | yes |  | yes | Unseen: platform_admin gets actions:['invoke','register','*'] — a wildcard hidden in a finite rule. |
