services:

  # ── State ─────────────────────────────────────────────────────────────────────

  redis-stack:
    image: redis/redis-stack-server:7.4.0-v3
    container_name: meridian-redis
    ports:
      - "6379:6379"
      - "8001:8001"    # RedisInsight (optional dev UI)
    volumes:
      - redis-data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 10
    restart: unless-stopped

  mongodb:
    image: mongo:8.0.20
    container_name: meridian-mongodb
    command: mongod --noauth
    volumes:
      - mongo-data:/data/db
    healthcheck:
      test: echo 'db.runCommand("ping").ok' | mongosh localhost:27017/LibreChat --quiet
      interval: 10s
      timeout: 5s
      retries: 10
    restart: unless-stopped

  # ── Observability stack (always-on — user wants full Grafana/OTel from the start) ──

  otel-collector:
    image: otel/opentelemetry-collector-contrib:latest
    container_name: meridian-otel
    volumes:
      - ./infra/otel-collector.yaml:/etc/otelcol-contrib/config.yaml:ro
    ports:
      - "4317:4317"   # OTLP gRPC
      - "4318:4318"   # OTLP HTTP
      - "8889:8889"   # Prometheus metrics scrape endpoint
    restart: unless-stopped

  tempo:
    image: grafana/tempo:2.6.1
    container_name: meridian-tempo
    command: ["-config.file=/etc/tempo.yaml"]
    volumes:
      - ./infra/tempo.yaml:/etc/tempo.yaml:ro
      - tempo-data:/var/tempo
    ports:
      - "3200:3200"   # Tempo HTTP query API
      - "4319:4317"   # OTLP gRPC (host port offset to avoid conflict)
    restart: unless-stopped

  prometheus:
    image: prom/prometheus:v2.55.1
    container_name: meridian-prometheus
    volumes:
      - ./infra/prometheus.yml:/etc/prometheus/prometheus.yml:ro
    ports:
      - "9090:9090"
    restart: unless-stopped

  grafana:
    image: grafana/grafana:11.3.1
    container_name: meridian-grafana
    ports:
      - "3000:3000"
    environment:
      GF_SECURITY_ADMIN_PASSWORD: meridian
      GF_SECURITY_ADMIN_USER: admin
      GF_USERS_ALLOW_SIGN_UP: "false"
    volumes:
      - grafana-data:/var/lib/grafana
      - ./infra/grafana/provisioning:/etc/grafana/provisioning:ro
    depends_on:
      - prometheus
      - tempo
    restart: unless-stopped

  # ── Gateway (Java/Spring Boot — the thing we're building) ────────────────────

  gateway:
    build:
      context: ./gateway
      dockerfile: Dockerfile
    container_name: meridian-gateway
    ports:
      - "8080:8080"
    environment:
      ZAI_API_KEY: ${ZAI_API_KEY}
      SPRING_DATA_REDIS_HOST: redis-stack
      SPRING_DATA_REDIS_PORT: 6379
      MERIDIAN_EMBEDDING_PROVIDER: remote
      MERIDIAN_EMBEDDING_REMOTE_URL: http://embeddings:8083/v1/embeddings
      MERIDIAN_EMBEDDING_DIMENSION: 384
      # OTel export endpoint (within compose network)
      OTEL_EXPORTER_OTLP_ENDPOINT: http://otel-collector:4318
      MANAGEMENT_OTLP_TRACING_ENDPOINT: http://otel-collector:4318/v1/traces
      MANAGEMENT_OTLP_METRICS_EXPORT_URL: http://otel-collector:4318/v1/metrics
      CERBOS_HOST: cerbos
      CERBOS_HTTP_PORT: 3592
    depends_on:
      redis-stack:
        condition: service_healthy
      embeddings:
        condition: service_healthy
      cerbos:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8080/actuator/health"]
      interval: 10s
      timeout: 5s
      retries: 15
      start_period: 30s
    restart: unless-stopped

  # ── Embedding sidecar (sentence-transformers all-MiniLM-L6-v2, 384-dim) ──────

  embeddings:
    build:
      context: ./mock-agents/embeddings
      dockerfile: Dockerfile
    container_name: meridian-embeddings
    ports:
      - "8083:8083"
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8083/health')"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s
    restart: unless-stopped

  # ── Mock agents (Domain 1: Wealth HTTP / Domain 2: Asset Servicing MCP) ──────

  wealth-http:
    build:
      context: ./mock-agents/wealth
      dockerfile: Dockerfile
    container_name: meridian-wealth-http
    ports:
      - "8081:8081"
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8081/health')"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  servicing-mcp:
    build:
      context: ./mock-agents/servicing
      dockerfile: Dockerfile
    container_name: meridian-servicing-mcp
    ports:
      - "8082:8082"
    environment:
      # Fault knobs — override in tests; defaults leave all tools healthy
      MCP_FAULT_TOOL: ""
      MCP_FAULT_ALL: "false"
      MCP_FAULT_DELAY_MS: "0"
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8082/health')"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 15s
    restart: unless-stopped

  # ── Chat UI (LibreChat — config + cosmetic only; no code fork) ────────────────

  librechat:
    image: registry.librechat.ai/danny-avila/librechat-dev:latest
    container_name: meridian-librechat
    ports:
      - "3080:3080"
    environment:
      HOST: "0.0.0.0"
      PORT: "3080"
      MONGO_URI: "mongodb://mongodb:27017/LibreChat"
      SEARCH: "false"
      MEILI_NO_ANALYTICS: "true"
      JWT_SECRET: ${JWT_SECRET:-meridian-demo-jwt-secret-change-in-production}
      JWT_REFRESH_SECRET: ${JWT_REFRESH_SECRET:-meridian-demo-jwt-refresh-secret-change-in-production}
      ALLOW_REGISTRATION: "true"
      ALLOW_SOCIAL_LOGIN: "false"
      NO_INDEX: "true"
    volumes:
      - ./.env:/app/.env:ro
      - ./librechat/librechat.yaml:/app/librechat.yaml:ro
      - librechat-uploads:/app/uploads
      - librechat-images:/app/client/public/images
    depends_on:
      mongodb:
        condition: service_healthy
      gateway:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3080/"]
      interval: 15s
      timeout: 10s
      retries: 20
      start_period: 45s
    restart: unless-stopped

  # ── Cerbos PDP — policy enforcement point for ABAC entitlements ──────────────
  cerbos:
    image: ghcr.io/cerbos/cerbos:latest
    container_name: meridian-cerbos
    ports:
      - "3594:3592"   # HTTP API (host 3594 to avoid conflict with other cerbos instances)
      - "3595:3593"   # gRPC
    volumes:
      - ./infra/cerbos:/conf:ro
    command: server --config=/conf/config.yaml
    healthcheck:
      test: ["CMD", "/cerbos", "healthcheck", "--insecure"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 10s
    restart: unless-stopped

  # ── User Management Service — separate admin plane (JWT issuance + user CRUD) ─
  user-mgmt:
    build:
      context: ./user-mgmt
      dockerfile: Dockerfile
    container_name: meridian-user-mgmt
    ports:
      - "8084:8084"
    environment:
      REDIS_HOST: redis-stack
      REDIS_PORT: 6379
      JWT_SECRET: ${JWT_SECRET:-meridian-demo-jwt-secret-change-in-production}
    depends_on:
      redis-stack:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8084/health')"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 15s
    restart: unless-stopped

  # ── Glass-box live decision trace panel ───────────────────────────────────────
  glassbox:
    build:
      context: ./glassbox
      dockerfile: Dockerfile
    container_name: meridian-glassbox
    ports:
      - "4000:4000"
    depends_on:
      - gateway
    restart: unless-stopped

  # ── k6 load test — Phase 7 only (scale profile) ───────────────────────────────

  k6:
    image: grafana/k6:latest
    container_name: meridian-k6
    profiles: [scale]
    volumes:
      - ./loadtest:/scripts
    environment:
      K6_OUT: "experimental-prometheus-rw=http://prometheus:9090/api/v1/write"
    command: run /scripts/load-test.js

volumes:
  redis-data:
  mongo-data:
  librechat-uploads:
  librechat-images:
  grafana-data:
  tempo-data:
