apiVersion: api.cerbos.dev/v1
# TENANT "default" — TOTAL child for resource `relationship` (Axiom Story B2.6).
# Reproduces today's decisions; effective = child ALLOW ∧ base ceiling. See
# tenant_default_agent.yaml for the full posture note. Fresh tenants use the deny-all template.
resourcePolicy:
  version: "default"
  resource: relationship
  scope: "default"
  scopePermissions: SCOPE_PERMISSIONS_REQUIRE_PARENTAL_CONSENT_FOR_ALLOWS
  rules:
    - actions: ["read"]
      effect: EFFECT_ALLOW
      roles: ["chat_user", "relationship_manager"]
    - actions: ["read"]
      effect: EFFECT_ALLOW
      roles: ["domain_admin"]
    - actions: ["read"]
      effect: EFFECT_ALLOW
      roles: ["platform_admin"]
