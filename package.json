{
  "name": "conduit-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Conduit / Axiom monorepo — the Axiom Admin console.",
  "workspaces": [
    "apps/admin"
  ],
  "scripts": {
    "build:admin": "npm run build --workspace axiom-admin",
    "build": "npm run build:admin"
  }
}
